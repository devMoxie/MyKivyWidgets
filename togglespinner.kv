<ToggleSpinner>:
     markup: True
     text: self.open_text if not self.is_open else self.close_text
     size_hint: (None, None)
     width: dp(175)
     height: dp(40)
     font_size: '16sp'

<SpinnerBtns>:
     font_size: '16sp'
     group: 'btns'
